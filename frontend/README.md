# frontend

Simple React + Vite frontend for the API Library project.

- Start dev server: `npm run dev` inside the `frontend` folder.
- Use `.env.local` to set `VITE_API_KEY` for development (optional).
- Styling uses Tailwind + DaisyUI; component utilities are in `src/styles/main.css`.
